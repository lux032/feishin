/**
 * Plex-branch fork configuration.
 *
 * This file only exists in the plex branch and holds values that intentionally
 * diverge from the upstream jeffvli/feishin repository.  Keep it minimal — one
 * constant per intentional divergence so merge conflicts stay isolated here
 * rather than scattered through shared files.
 */

/** GitHub owner used by electron-updater to point auto-update at this fork. */
export const UPDATER_OWNER = 'lux032';
